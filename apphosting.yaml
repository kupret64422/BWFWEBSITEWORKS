# apphosting.yaml
# This file configures your Firebase App Hosting backend.

build:
  command: "npm install"
  outputDirectory: "."

env:
  - variable: FIREBASE_SERVICE_ACCOUNT
    secret: my_id # <--- CHANGE THIS LINE!
    availability:
      - RUNTIME